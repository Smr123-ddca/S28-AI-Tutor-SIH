const fs = require('fs'); const retrieval = require('./src/services/retrieval.service'); const store = require('./src/data/store'); store.loadData(); console.log('Chunks Loaded:', store.getChunks().length); const res1 = retrieval.retrieve('The Problem Solving Approach (PSA)', { course: 'PSA' }); console.log('Retrieve 1:', res1); const res2 = retrieval.retrieve('What does the Problem Solving Approach break tasks down into?', { course: 'PSA' }); console.log('Retrieve 2:', res2); const res3 = retrieval.retrieve('What is iterative refinement?', { course: 'PSA' }); console.log('Retrieve 3:', res3);
