const r = require('./src/services/retrieval.service'); require('./src/data/store').loadData(); const exp = require('./src/utils/queryExpander'); function test(msg) { const tokens = exp.buildRetrievalQuery({userMessage:msg}); const sExp = r.retrieve(msg, {tokens: tokens.expandedTokens, course:'PSA'}); const sNat = r.retrieve(msg, {course:'PSA'}); console.log('MSG:', msg); console.log('- Tokens:', tokens.expandedTokens); console.log('- Score Exp:', sExp[0]?.score); console.log('- Score Nat:', sNat[0]?.score); } test('The Problem Solving Approach (PSA) emphasizes breaking down complex tasks into atomic units.'); test('What does the Problem Solving Approach break tasks down into?'); test('What is iterative refinement?');
